# Changesets

Add a changeset for any consumer-facing change: `npx changeset`, pick a bump
(patch/minor/major), and describe it. Merging to `main` triggers the release
workflow, which versions and publishes `@ash2k5/cinematic-ds` to GitHub Packages.
